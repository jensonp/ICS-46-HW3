#include "../src/sorted_list.h"
#include <iostream>

int main() {
    //std::string input_file = "src/random.txt";
    std::string input_file = "src/words.txt";
    measure_lists(input_file); 
    return 0;
}